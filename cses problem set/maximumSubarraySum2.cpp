#include <iostream>
#include <vector>
using ll = long long;
using namespace std;

int main(){

    int n,a,b;
    cin>>n>>a>>b;

    vector<ll>v(n,0);
    for(auto&it:v){
        cin>>it;
    }

    
    
    return 0;
}